[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dmapagent"
version = "0.1.0"
description = "LLM-powered data mapping agent"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["src*"]
